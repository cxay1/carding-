[User] → [CDN / Frontend (React/Vue)] → [API Gateway] → [Backend Services]
                                                             ↓
                                           [Payment Gateway] → [Card Inventory DB]
                                                             ↓
                                           [Redeem Service] → [Third-party APIs (e.g., Google Play)]
